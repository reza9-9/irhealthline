name: Auto Medical Content Generator Pro

on:
  schedule:
    - cron: '0 4 * * *'  # هر روز ساعت ۸ صبح ایران
  workflow_dispatch:

jobs:
  generate-and-publish:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4 pandas
        echo "✅ وابستگی‌ها نصب شد"
      
    - name: Run medical bot
      run: python medical_bot.py
      
    - name: Generate dashboard
      run: python dashboard.py
      
    - name: Generate analytics reports
      run: python analytics.py
      
    - name: Upload results
      uses: actions/upload-artifact@v4
      with:
        name: medical-content-results
        path: |
          medical_content.db
          index.html
          auto_articles_*.json
          weekly_report_*.json
        retention-days: 30
        
    - name: Show success message
      run: |
        echo "🎉 سیستم با موفقیت اجرا شد!"
        echo "📊 داشبورد: index.html"
        echo "📈 گزارش هفتگی: weekly_report_*.json"
        echo "🗄️ دیتابیس: medical_content.db"
        echo "📄 مقالات: auto_articles_*.json"
